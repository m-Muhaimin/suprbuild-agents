# Server
PORT=3001
NODE_ENV=production
BASE_URL=https://yourdomain.com

# Database
DATABASE_URL=postgresql://suprbuild:YOUR_PASSWORD@localhost:5432/suprbuild

# CORS
CORS_ORIGIN=*

# Rate Limiting (requests per 15min window)
# RATE_LIMIT_MAX=200
